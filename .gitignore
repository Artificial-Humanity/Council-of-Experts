# Rust build products
/target/

# Xcode/SwiftPM build products
.build/
.swiftpm/
build/
DerivedData/

# OS-level
.DS_Store

# App bundle
/CouncilOfExperts.app/

.obsidian/
# Working notes are private and live in the org Notes repo; `notes` is a
# symlink to Notes/Council-of-Experts. It must never be committed here.
/notes
